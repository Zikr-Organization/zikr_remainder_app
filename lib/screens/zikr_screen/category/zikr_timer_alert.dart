import 'package:flutter/material.dart';

class ZikrTimerAlert extends StatelessWidget {
  const ZikrTimerAlert({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
